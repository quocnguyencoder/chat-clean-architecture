# WebSocket Configuration (Optional)
# Leave empty to use demo mode without a server

# WebSocket server URL (e.g., wss://your-server.com/ws)
VITE_WEBSOCKET_URL=

# Authentication token (if your server requires it)
VITE_AUTH_TOKEN=

# Enable demo mode (set to true for testing without a server)
VITE_ENABLE_DEMO=false
