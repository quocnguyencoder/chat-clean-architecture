/**
 * URL Parameters Constants
 *
 * Centralized constants for URL query parameters and routing.
 */

/**
 * URL query parameter keys
 */
export const URL_PARAMS = {
  /**
   * Chat ID parameter key
   */
  CHAT_ID: 'chatId',

  /**
   * Message ID parameter key
   */
  MESSAGE_ID: 'messageId',
} as const;
